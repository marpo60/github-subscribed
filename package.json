{
  "scripts": {
    "test": "eslint .",
    "release": "webstore upload --source=ext --auto-publish",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "@octokit/rest": "^15.9.4",
    "chrome-webstore-upload-cli": "^1.1.1",
    "cypress": "^3.0.2",
    "eslint": "^3.19.0",
    "install": "^0.12.1"
  }
}
